apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-aws-official
spec:
  package: xpkg.upbound.io/upbound/provider-aws-staging:v0.3.0
  packagePullSecrets:
    - name: official-pull-secret
